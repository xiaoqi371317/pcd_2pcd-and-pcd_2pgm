# pcd_2pcd-pcd2pgm
Browser-based ROS2 navigation toolkit for PCD relocalization, occupancy map editing, waypoint planning, and rigid point cloud transforms. Supports interactive 3D LiDAR pose adjustment, map origin resetting, and ROS-compatible PGM/YAML + transformed PCD export.
